const runLevel = monsters =>
  new Promise((resolve, reject) => {
    const player = new Player();

    /**
     * @type {Bullet[]}
     */
    const bullets = [];

    /**
     * @type {Monster[]}
     */
    const monsters = [];

    /**
     * @type {Explosion}
     */
    const explosions = [];

    resolve(true);
  });
